//TODO: Use proper values for dev and production
let config = {
    baseUrl: 'http://localhost:8080',
    loginUrl: 'api/auth',
    tokenName: 'cerberus_token',
    loginRedirect: '#/',
    responseTokenProp: 'token'
};

export default config;
